import React from 'react';
import Dropdown from 'react';
import DropdownButton from 'react';

function run() {
    const[result.getdata]=useState([]) {
        usEffect(() =>
            fetch('https://us17.api.mailchimp.com/3.0/')
        )
    }
}
